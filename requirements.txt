# EndoSeg — top-level dev/training dependencies (pinned majors for reproducibility).
# Per-component minimal requirements live in jobs/*/requirements.txt,
# endpoint/requirements.txt and proxy/requirements.txt.
torch>=2.2
monai>=1.3
albumentations>=1.4
onnx>=1.16
onnxruntime>=1.18
opencv-python>=4.9
numpy>=1.26
pillow>=10.0
matplotlib>=3.8
tqdm>=4.66

# Dev / testing
pytest>=8.0
